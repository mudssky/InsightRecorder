# 安全与监控

## 目标

- Electron 安全基线与日志/崩溃监控。

## 范围

- `contextIsolation: true`、`sandbox: true`；仅 `preload` 暴露白名单；
- `electron-log` 与 Sentry Electron SDK。

## 执行步骤

- IPC Schema 校验与最小权限；限制 `file://` 访问范围；错误上报通道。

## 验收标准

- 安全检查通过；关键异常有日志与上报。
