# 备份与同步（M3）

## 目标

- 本地/NAS 自动备份与增量策略；删除走回收站可恢复。

## 范围

- 定时任务：`node-cron`；文件操作：`fs`；指纹与时间戳策略；压缩：`archiver`。

## 执行步骤

- 备份策略配置（实时/定时）；增量仅复制新增或改动文件。
- 回收站目录隔离与保留期限；恢复操作。

## 验收标准

- 定时备份按计划执行；增量同步准确；回收站可恢复。
