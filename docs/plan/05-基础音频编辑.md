# 基础音频编辑（M4）

## 目标

- 剪辑、降噪、人声增强、格式转换；并发与重试可控。

## 范围

- `ffmpeg-static` + `fluent-ffmpeg`；静音去除 `silenceremove`；降噪 `afftdn`/`anlmdn`；
- 元数据：`music-metadata`；任务队列：`p-queue`。

## 执行步骤

- 可视化截取区间并生成新片段；批量处理时队列并发控制与失败重试。
- 转码到 MP3/WAV 等；保留原始文件。

## 验收标准

- 生成片段与转码成功；降噪有效；高并发稳定无阻塞。
